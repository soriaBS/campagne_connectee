{% extends 'base.html.twig' %}

{% block main %}
	{{ produit.nom }}
    <img src="{{ asset("images/produits/"~produit.images[0].libelle ) }}" width="10%" height="10%"/>
	{{ produit.type.libelle }}
	{{ produit.prix }}
{% endblock %}

{% block titre %}

{% endblock %}